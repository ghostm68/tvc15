name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        
    - name: Install dependencies and build
      run: |
        npm install --legacy-peer-deps
        npm run build
        
    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Initialize Capacitor and add Android
      run: |
        rm -f capacitor.config.ts
        mkdir -p www
        cp -r dist/* www/
        npx @capacitor/cli init "tvc15" "com.tvc15.app"
        npx @capacitor/cli add android
        
    - name: Replace Android icons and disable adaptive icons
      run: |
        # Remove all the adaptive icon stuff
        rm -rf android/app/src/main/res/drawable-*
        rm -rf android/app/src/main/res/mipmap-anydpi-v26
        
        # Create simple mipmap directories
        mkdir -p android/app/src/main/res/mipmap-mdpi
        mkdir -p android/app/src/main/res/mipmap-hdpi
        mkdir -p android/app/src/main/res/mipmap-xhdpi
        mkdir -p android/app/src/main/res/mipmap-xxhdpi
        mkdir -p android/app/src/main/res/mipmap-xxxhdpi
        
        # Copy our icon to all densities
        cp icon.png android/app/src/main/res/mipmap-mdpi/ic_launcher.png
        cp icon.png android/app/src/main/res/mipmap-hdpi/ic_launcher.png
        cp icon.png android/app/src/main/res/mipmap-xhdpi/ic_launcher.png
        cp icon.png android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
        cp icon.png android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
        
        # Also create round versions (use same icon)
        cp icon.png android/app/src/main/res/mipmap-mdpi/ic_launcher_round.png
        cp icon.png android/app/src/main/res/mipmap-hdpi/ic_launcher_round.png
        cp icon.png android/app/src/main/res/mipmap-xhdpi/ic_launcher_round.png
        cp icon.png android/app/src/main/res/mipmap-xxhdpi/ic_launcher_round.png
        cp icon.png android/app/src/main/res/mipmap-xxxhdpi/ic_launcher_round.png
        
    - name: Update AndroidManifest to use simple icons
      run: |
        # Make sure Android uses our simple icons instead of adaptive icons
        sed -i 's/android:icon="@mipmap\/ic_launcher"/android:icon="@mipmap\/ic_launcher"/' android/app/src/main/AndroidManifest.xml
        sed -i 's/android:roundIcon="@mipmap\/ic_launcher_round"/android:roundIcon="@mipmap\/ic_launcher_round"/' android/app/src/main/AndroidManifest.xml
        
    - name: Sync Capacitor
      run: npx @capacitor/cli sync
      
    - name: Build Android APK
      run: |
        cd android
        chmod +x ./gradlew
        ./gradlew assembleDebug
      
    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: tvc15-app
        path: android/app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30
